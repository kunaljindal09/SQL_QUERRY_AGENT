# Development & CI — install after app deps:
#   pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0,<9
pytest-asyncio>=0.24.0,<1
pytest-benchmark>=4.0.0,<5
aiosqlite>=0.19.0,<1
sqlparse>=0.5.0,<1
